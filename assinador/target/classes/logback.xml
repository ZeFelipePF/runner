<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- JSON structured output to stderr -->
    <appender name="STDERR_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service.name":"assinador","service.version":"0.1.0"}</customFields>
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>
        </encoder>
    </appender>

    <!-- OpenTelemetry appender para futuro OTLP export -->
    <appender name="OTEL"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
    </appender>

    <root level="INFO">
        <appender-ref ref="STDERR_JSON" />
        <appender-ref ref="OTEL" />
    </root>
</configuration>
